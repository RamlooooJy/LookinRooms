import {FC} from "react";
import Button from "../Button";

const DatePicker:FC = ()=>{
  return <Button className='datepicker button flex-cc'>
    <div>132</div>
  </Button>
}

export default DatePicker
